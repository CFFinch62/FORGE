# math_utils.fg — user module for multi-file import test
# Loaded via:  import math_utils  in the main file

proc double(n: int) -> int:
    return n * 2

proc add(a: int, b: int) -> int:
    return a + b

proc greeting() -> str:
    return "Hello from math_utils module!"
